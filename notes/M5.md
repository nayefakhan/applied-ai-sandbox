# M5 — Task Decomposition

## Decomposition (8-10 tasks)
1. **Task 1 — <initialtag>**: create a "tag", a string of text stored as a new key on the dict. 
Verify: edit the code and have it include a string variable that should be printed out as a tag.
2. **Task 2 — <input>**: allow for user input when the page first loads. 
Verify: commit changes, then open localhost:5000
3. **Task 3 — <edgecase>**: handle empty strings. 
Verify: user will simply press enter without any input
4. **Task 4 — <changetags>**: allow for the user to be able to input data even after the initial page load 
Verify: add tag once when the page loads, then add another after the app finishes loading
5. **Task 5 — <othertags>**: allow for multiple tags, each stored in the dict and displayed. 
Verify: give a series of various inputs, including empty strings for edge case checking
6. **Task 6 — <multipletagged>**: let the user add tags for different notes 
Verify: create two notes, then add a tag to each one

## Task 1 context brief
Goal: Accessing the in-memory list of dicts inside of the sandbox, add a new key titled tags, which corresponds to a string. 
Constraints: The default value is an empty string
Files: app.py should be edited, specifically app.notes: list[dict] = [], to include a key-value pair where the key is named tags and the string is empty.
Acceptance: run app.py, then replace the empty string in app.notes with "test". check with all existing tests.

Read the relevant files and do not write code until told to do so.

## Task 1 — what happened
- Claude's plan summary: The sandbox app has no auth. All notes are in a single global in-memory list
(app.notes) visible to everyone. The goal is to let users register,
and log out, and to scope each user's note list to their own account — while
keeping the existing test suite green.
- What I pushed back on: Claude initially implemented broken code for making the tag visible, but I decided to save visible tags for a different step.
- Verification result: Claude changed app.py correctly.

## Task 2 context brief

Goal: add a line of code within def new_note(): and change the empty string assigned to the key "tags" into a variable named tags
Constraints: First edit def new_note(): THEN edit the key "tags"
Files: app.py should be edited, specifically including a new line of the format tags = (request.form.get("tags") or "").strip()
Acceptance: run app.py, check with all existing tests.

Read the relevant files and do not write code until told to do so.

## Task 2 — what happened
- It did not work

## Task 3 — what happened
- ...

## Reflection — where I redirected
- (3 sentences)
Claude did not implement user input for the code, leading to me redoing the turn. This made me check why it was not working, leading to the realization that in fact, to accept tags the tests themselves would have to be changed. As I am not allowed to do that, I left it as is, but I am under the belief that Claude functions perfectly under the context.