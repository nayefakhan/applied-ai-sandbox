# M4 — Reading and Verifying Output

## Bug hunt #1 — SQL injection
- Bug: CLaude directly adds q, a variable input by the user, into the SQL string, which can be exploited by malicious actors
- Why Claude wrote it: Claude interpreted it as a Python f-string
- Fix: Specify the parameters of the SQL string

## Bug hunt #2 — hallucinated package
- Bug: flask_simpleauth is not a real package, it should be Flask-Login
- Why Claude wrote it: Like many LLMs, Claude concocted up fake information that sounds about right
- Fix: Replace with Flask-Login

## Bug hunt #3 — edge case
- Bug: Valid input that cannot be processed by the code
- Why Claude wrote it: It chose an ideal scenario instead of an edge case.
- Fix: Add statement to catch this option, and test it

## Bug hunt #4 — hardcoded secret
- Bug: Will expose the API key
- Why Claude wrote it: Claude simply inputs values, it has no further throught.
- Fix: Call the key into the function without actually writing it in.

## Test I wrote
- Bug I picked: edge case
- Test code: assert average_rating([8, 7, ]) == 
- Confirmed it fails on the bug and passes on the fix? (yes)

## Near-miss reflection
- Claude produced a line whose argument called itself; I realized how nonsensical this was when I was about to commit. This would be prevented if I had already been reviewing my code.